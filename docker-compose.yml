name: tilawah

services:
  api:
    build:
      context: ./server
    container_name: tilawah-api
    ports:
      - "8010:8000"          # local dev; on the VPS, Caddy is the public entry
    environment:
      MODEL_ID: ${MODEL_ID:-/root/.cache/huggingface/quran-ct2}
      DEVICE: ${DEVICE:--1}
      REDIS_URL: redis://redis:6379/0
      DATABASE_URL: postgresql://tilawah:${POSTGRES_PASSWORD:-tilawah}@db:5432/tilawah
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change-me}
      HF_HUB_DISABLE_SYMLINKS_WARNING: "1"
      AUDIO_DIR: ${AUDIO_DIR:-/data/audio}
      ADMIN_EMAILS: ${ADMIN_EMAILS:-}
      ASR_BACKEND: ${ASR_BACKEND:-local}
      ASR_BEAM_SIZE: ${ASR_BEAM_SIZE:-5}
      MODAL_ASR_NAME: ${MODAL_ASR_NAME:-}
      MODAL_TOKEN_ID: ${MODAL_TOKEN_ID:-}
      MODAL_TOKEN_SECRET: ${MODAL_TOKEN_SECRET:-}
    volumes:
      # Cache the downloaded model so it survives container restarts/reboots.
      - tilawah-models:/root/.cache/huggingface
      # Persist collected training recitations across redeploys.
      - tilawah-audio:/data/audio
    depends_on:
      redis:
        condition: service_started
      db:
        condition: service_healthy
    restart: unless-stopped

  web:
    build:
      context: ./web
    container_name: tilawah-web
    depends_on:
      - api
    restart: unless-stopped

  caddy:
    image: caddy:2-alpine
    container_name: tilawah-caddy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      - api
      - web
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: tilawah-redis
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    container_name: tilawah-db
    environment:
      POSTGRES_USER: tilawah
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-tilawah}
      POSTGRES_DB: tilawah
    volumes:
      - tilawah-db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U tilawah -d tilawah"]
      interval: 5s
      timeout: 5s
      retries: 12
    restart: unless-stopped

volumes:
  tilawah-models:
  tilawah-audio:
  tilawah-db:
  caddy-data:
  caddy-config:
